{
  "name": "snowflake",
  "private": true,
  "version": "1.0.0",
  "description": "p5.js を使った TextAlive App API の作例",
  "main": "index.js",
  "scripts": {
    "dev": "parcel src/index.html --out-dir dev",
    "build": "parcel build src/index.html --public-url ./ --out-dir dist --no-source-maps",
    "clean": "del-cli .cache dev dist",
    "predeploy": "rm -rf dist && parcel build index.html --public-url YOUR_REPO_NAME",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [
    "textalive"
  ],
  "author": "Jun Kato <i@junkato.jp> (https://junkato.jp), Takeshi Ishita <ishitah.takeshi@gmail.com> (https://ishitatakeshi.netlify.com/)",
  "license": "MIT",
  "dependencies": {
    "p5": "^1.1.9",
    "textalive-app-api": "^0.1.1"
  },
  "devDependencies": {
    "del-cli": "^3.0.1",
    "gh-pages": "^3.1.0",
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-clean-dist": "0.0.6"
  }
}
